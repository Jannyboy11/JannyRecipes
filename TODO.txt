Fix all TODOs in the code

enige dingen die ik nog moet doen zijn saven en loaden implementeren,
nog wat meer knoppen hun functionaliteit geven,
checken of alles goed werkt met het recipe book van de client (zo nee, daar nog packets voor sturen en zo),
en modifiers (zoals nbt-specificness,
ingrediënten met een bepaalde count),
en de gui voor meerdere materials per ingrediënt.

split project up into modules per server implementation
use macros too: make a separate module for macros

