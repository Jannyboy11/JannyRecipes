package xyz.janboerman.recipes.api.persist

trait RecipeStorage {

    //saveRecipe(key: NamespacedKey /*is this parameter necessary?*/, recipe: Recipe): Option[String] //None on succes, Some(errorMessage) on failure

    //loadRecipe[R <: Recipe](key: NamespacedKey): Option[R]

    //deleteRecipe(key: NamespacedKey)

}
